import 'package:flutter/material.dart';

class AppColors {
  // Emerald / Forest Green Primary Palette
  static const Color primary = Color(0xFF059669);
  static const Color primaryDark = Color(0xFF064E3B);
  static const Color primaryLight = Color(0xFF10B981);
  static const Color primarySurface = Color(0xFFECFDF5);
  static const Color primaryAccent = Color(0xFF34D399);

  // Amber / Harvest Gold Secondary Palette
  static const Color accentGold = Color(0xFFF59E0B);
  static const Color accentAmber = Color(0xFFD97706);
  static const Color goldLight = Color(0xFFFEF3C7);

  // Dark Tech Slates
  static const Color slateDark = Color(0xFF0F172A);
  static const Color slateCard = Color(0xFF1E293B);
  static const Color slateBorder = Color(0xFF334155);

  // Light Backgrounds & Neutrals
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFF1F5F9);

  // Status & Verification Badges
  static const Color verifiedGreen = Color(0xFF10B981);
  static const Color pendingAmber = Color(0xFFF59E0B);
  static const Color flaggedRed = Color(0xFFEF4444);
  static const Color infoBlue = Color(0xFF3B82F6);

  // Text Colors
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color textMuted = Color(0xFF94A3B8);
  static const Color textOnDark = Color(0xFFF8FAFC);
}
